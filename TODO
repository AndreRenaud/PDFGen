* Improve barcode type support
* Change media size to be per page (defaults to those from pdf_create if not specified).
* Allow sub-point locations - use floats instead of integer positioning?
* Add support for utf-8 (at the moment it's 7-bit ASCII only, especially around font width calculations).
